<li>
	{{burger_name}}
    
    {{#unless devour}}
        <button class="btn btn-danger devour-button shadow" data-id="{{id}}" data-newdevour="{{devoured}}"> 
            NOM NOM NOM
        </button>
    {{/unless}}

</li>